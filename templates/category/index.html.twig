
{% extends 'base.html.twig' %}
{% block title %}{{ category.name |capitalize }}{% endblock %}
{% block body %}
    <div class="container mx-auto">
    <h1 class="text-center text-5xl mt-24 mb-8">Categorie : {{ category.name |capitalize}}</h1>
    <div class="flex flex-row flex-wrap justify-evenly mt-8">
        {% for post  in category.posts  %}
            {% include('components/_card.html.twig') %}


        {% endfor %}

    </div>

{% endblock %}